Place the portfolio owner's portrait image here with the filename `abdosaid.jpg` (or `abdosaid.png`).
Recommended size: 800×1000 (or a tall crop). The HTML uses `assets/abdosaid.jpg` as the primary image and will fall back to `assets/placeholder.svg` if the photo is missing.

If you want me to embed the exact image you uploaded, tell me and I will add it to the project as `assets/abdosaid.jpg`.